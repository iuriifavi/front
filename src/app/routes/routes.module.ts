import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';

import { MainRoutes } from './mainRoutes';

@NgModule({
  imports: [
    CommonModule,
    MainRoutes
  ],
  declarations: [ ]
})

export class ShansRoutes { }
